/**
 * 覆蓋quasar預設變數（顏色為主）
 * 注意：勿更改或刪除變數名稱，僅更改值
 * https://quasar-framework.org/components/stylus-variables.html
 */
$primary   = #0022ae
$secondary = #022169

/**
 * 自訂顏色（只有king001版特別加）
 */
$primary-active = #004EC7

/**
 * 共用圖片路徑
 */
$styleFolder = $path + 'statics/king/king001/'
$styleUrl(v)
    url($styleFolder + (v))